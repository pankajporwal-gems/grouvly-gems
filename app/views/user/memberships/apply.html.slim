- content_for :title do
  = t('user.memberships.apply.submit_your_application')

.background-gray
  .container
    .block-form.col-lg-12.clearfix
      h3= t('user.memberships.edit.edit_your_information')
      
      = form_for(@presenter.membership, url: submit_application_user_membership_path, html: { role: 'form' }) do |f|
        = render partial: 'user/memberships/form', locals: { presenter: @presenter, f: f,
          btn: t('user.memberships.apply.submit_your_application') }
